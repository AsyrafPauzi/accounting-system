import { useState } from 'react'; /** * Disclosure row for FAQ sections. Click to expand. Used on the pricing page. */
export default function FAQRow({ question, answer, defaultOpen = false }) { const [open, setOpen] = useState(defaultOpen); return ( <div className="border-b border-border-warm py-4 last:border-b-0"> <button type="button" onClick={() => setOpen(!open)} className="w-full flex items-start justify-between text-left gap-4" aria-expanded={open} > <span className="font-display text-lg font-medium text-ink leading-snug"> {question} </span> <span className={`flex-shrink-0 mt-1 text-ink-muted transition-transform duration-200 ${open ? 'rotate-45' : ''}`}> <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"> <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 4v16m8-8H4" /> </svg> </span> </button> {open && ( <div className="mt-3 text-sm text-ink-muted leading-relaxed"> {answer} </div> )} </div> );
}
