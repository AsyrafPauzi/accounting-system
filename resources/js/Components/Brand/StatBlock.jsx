import EyebrowLabel from './EyebrowLabel'; /** * Small KPI card. Renders an eyebrow label, a large mono figure (already * formatted by the caller), and an optional caption underneath. */
export default function StatBlock({ label, value, caption, accent = 'surface', valueClassName = '', className = '',
}) { const accentClass = { surface: 'bg-surface border border-border-warm', forest: 'bg-forest text-white border-0 dark:bg-forest-light', cream: 'bg-cream border border-border-warm', terracotta: 'bg-terracotta/10 border border-terracotta/30', mustard: 'bg-mustard/15 border border-mustard/40', }[accent] || 'bg-surface border border-border-warm'; const labelTone = accent === 'forest' ? 'ink' : 'ink'; return ( <div className={`rounded-2xl p-4 sm:p-5 flex flex-col min-h-[100px] sm:min-h-[112px] ${accentClass} ${className}`}> <EyebrowLabel tone={labelTone} className={accent === 'forest' ? 'text-white/80' : 'text-ink-muted'} > {label} </EyebrowLabel> <p className={`mt-1 sm:mt-2 text-lg sm:text-2xl font-mono font-medium font-tabular truncate ${valueClassName}`}> {value} </p> {caption && ( <p className={`mt-auto text-[11px] ${accent === 'forest' ? 'text-white/80' : 'text-ink-muted'}`}> {caption} </p> )} </div> );
}
