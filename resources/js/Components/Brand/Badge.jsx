/** * Small badge for status pills. Tones map to brand semantic colours rather * than raw red/green/yellow, so dark mode and self-hosted overrides stay * coherent. */
export default function Badge({ children, tone = 'neutral', className = '', ...props }) { const toneClass = { neutral: 'bg-surface-alt text-ink border border-border-warm', success: 'bg-forest/10 text-forest dark:text-forest-light border border-forest/30', warning: 'bg-mustard/15 text-ink border border-mustard/40', danger: 'bg-terracotta/10 text-terracotta dark:text-terracotta-light border border-terracotta/30', info: 'bg-cream text-ink border border-border-warm', accent: 'bg-terracotta text-white border border-terracotta', }[tone] || 'bg-surface-alt text-ink border border-border-warm'; return ( <span className={`inline-flex items-center gap-1 rounded-full px-2.5 py-0.5 text-[11px] font-medium ${toneClass} ${className}`} {...props} > {children} </span> );
}
